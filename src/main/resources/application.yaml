spring:
  application:
    name: spring-board
  datasource:
    url: jdbc:mysql://localhost:3306/spring_board
    username: root
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
